# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
    paid_at:
        label: PaidAt
        disabled: true
    status:
        label: Status
        type: dropdown
        options:
            pendente: Pendente
            pago: Pago
        default: pendente
    client:
        label: Cliente
        type: relation
    products:
        label: produtos
        type: relation
